<UserControl x:Class="CafeClient.Presentation.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid>
        <StackPanel>
            <TextBlock Text="Клиенты" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <TextBox Width="220" Height="28" Margin="0,0,8,0" VerticalAlignment="Center"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Поиск" Height="28" Command="{Binding SearchCommand}" Margin="0,0,8,0"/>
                <Button Content="Добавить клиента" Height="28" Background="#4CAF50" Foreground="White" Padding="16,0"
                        Command="{Binding AddClientCommand}"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Clients}" AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                    <DataGridTextColumn Header="Уровень лояльности" Binding="{Binding LoyaltyLevelId}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
